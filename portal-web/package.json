{
  "name": "portal-web",
  "private": true,
  "version": "0.1.0",
  "description": "Portal Web application package with Laravel API and colocated Vue frontend",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview"
  }
}
