# Frontend env template.
#
# 重要：Vite 会把所有 VITE_* 变量打进前端 bundle。
# 因此严禁把以下任一项放进这里或运行时注入：
#   - ADMIN_SHARED_TOKEN / VITE_ADMIN_SHARED_TOKEN
#   - INTERNAL_SHARED_TOKEN / VITE_INTERNAL_SHARED_TOKEN
#   - BOOTSTRAP_SHARED_TOKEN / VITE_BOOTSTRAP_SHARED_TOKEN
#   - 任何 *_HMAC_KEY / *_SECRET
#
# 后台浏览器只持有 Sanctum bearer（来自 /api/v1/public/auth/admin-login 登录接口）。
# SRE 工具调用走 curl / 脚本，永不经过浏览器。
